# ESP-IDF build output
build/
managed_components/
sdkconfig.old

# Firmware artifacts
*.bin
*.elf
*.map

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Note: sdkconfig IS tracked on purpose — it documents the exact
# configuration used during the experiments (e.g. CONFIG_BLE_MESH_RELAY=y).
